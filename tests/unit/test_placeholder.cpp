/**
 * @file test_placeholder.cpp
 * @brief 占位符测试
 *
 * 用于验证构建管道是否正常工作的基础测试。
 * 当其他测试尚未实现时，确保测试框架可以正常编译和运行。
 */

#include <gtest/gtest.h>

TEST(PlaceholderTest, BuildPipelineIsReady)
{
    EXPECT_TRUE(true);
}
