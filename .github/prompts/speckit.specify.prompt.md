---
agent: speckit.specify
---

用于 Speckit 的第 1 阶段，生成或修订功能规格说明书。

- 适用时机：你已经有功能目标，需要把它落成 `spec.md`。
- 前置条件：建议先确认宪章有效；首次执行时可直接提供一句功能描述。
- 执行结果：创建或更新 `FEATURE_DIR/spec.md`，必要时只会提出创建首版 spec 所必需的最少问题，并保留需求 ID 与变更摘要。
- 边界：该阶段只定义需求，不负责系统设计和代码实现；深度歧义消解应交给 `speckit.clarify`。
- 典型输入：`实现带验证码和锁定策略的用户登录`。
